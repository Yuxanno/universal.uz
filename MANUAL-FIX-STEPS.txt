=== VPS DA BUGUNGI STATISTIKA MUAMMOSINI HAL QILISH ===

MUAMMO: Bugungi savdo statistikasi 0 ko'rsatyapti (lekin ma'lumotlar bazada bor)

SABAB: VPS serveri UTC vaqtida ishlaydi, O'zbekiston UTC+5 da

=== YECHIM 1: Faylni to'g'ridan-to'g'ri VPS da tahrirlash ===

1. VPS ga ulaning:
   ssh root@vmi3023927.contaboserver.net

2. Faylni ochish:
   nano /var/www/universalbozor/server/src/routes/stats.js

3. 9-17 qatorlarni quyidagicha o'zgartiring:

OLDINGI KOD (9-17 qatorlar):
```javascript
router.get('/', auth, authorize('admin'), async (req, res) => {
  try {
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const weekAgo = new Date(today);
    weekAgo.setDate(weekAgo.getDate() - 7);
    
    const monthAgo = new Date(today);
    monthAgo.setMonth(monthAgo.getMonth() - 1);
```

YANGI KOD:
```javascript
router.get('/', auth, authorize('admin'), async (req, res) => {
  try {
    // Uzbekistan timezone (UTC+5) - calculate today's start in Uzbekistan time
    const now = new Date();
    const uzbekistanOffset = 5 * 60 * 60 * 1000; // 5 hours in milliseconds
    const uzbekistanNow = new Date(now.getTime() + uzbekistanOffset);
    
    // Get today's start in Uzbekistan time, then convert back to UTC for MongoDB query
    const today = new Date(uzbekistanNow);
    today.setUTCHours(0, 0, 0, 0);
    const todayUTC = new Date(today.getTime() - uzbekistanOffset);
    
    const weekAgo = new Date(todayUTC);
    weekAgo.setDate(weekAgo.getDate() - 7);
    
    const monthAgo = new Date(todayUTC);
    monthAgo.setMonth(monthAgo.getMonth() - 1);
```

4. 28-qatorda `today` ni `todayUTC` ga o'zgartiring:
   OLDIN: Receipt.aggregate([{ $match: { status: 'completed', createdAt: { $gte: today } } }
   KEYIN: Receipt.aggregate([{ $match: { status: 'completed', createdAt: { $gte: todayUTC } } }

5. 35-qatorda ham timezone qo'shing:
   OLDIN: { $group: { _id: { $hour: '$createdAt' }, count: { $sum: 1 }, total: { $sum: '$total' } } },
   KEYIN: { $group: { _id: { $hour: { date: '$createdAt', timezone: '+05:00' } }, count: { $sum: 1 }, total: { $sum: '$total' } } },

6. Saqlash: Ctrl+O, Enter, Ctrl+X

7. Serverni restart qilish:
   pm2 restart universalbozor-api
   pm2 save

8. Tekshirish:
   curl http://localhost:5000/api/stats | jq '.todaySales'

9. VPS dan chiqish:
   exit


=== YECHIM 2: Yangi faylni to'liq yuklash ===

Windows PowerShell da:

cd C:\Users\ozodb\Desktop\loyihalar\universal.uz

# Faylni yuklash
scp server/src/routes/stats.js root@vmi3023927.contaboserver.net:/var/www/universalbozor/server/src/routes/stats.js

# VPS ga ulanish va restart
ssh root@vmi3023927.contaboserver.net

cd /var/www/universalbozor
pm2 restart universalbozor-api
pm2 logs universalbozor-api --lines 20

# API ni tekshirish
curl http://localhost:5000/api/stats | jq

exit


=== YECHIM 3: To'liq rebuild va deploy ===

Agar yuqoridagilar ishlamasa:

1. Localhost da build:
   cd C:\Users\ozodb\Desktop\loyihalar\universal.uz
   npm run build

2. Barcha fayllarni yuklash:
   scp -r server/src root@vmi3023927.contaboserver.net:/var/www/universalbozor/server/
   scp -r client/dist/* root@vmi3023927.contaboserver.net:/var/www/universalbozor/client/dist/

3. VPS da restart:
   ssh root@vmi3023927.contaboserver.net "cd /var/www/universalbozor && pm2 restart universalbozor-api && pm2 save"


=== TEKSHIRISH ===

Brauzerda: https://pos.universalbozor.uz
- Dashboard ni oching
- Bugungi savdo raqamini tekshiring
- Agar 0 bo'lsa, Ctrl+Shift+R (hard refresh)


=== DEBUG ===

Agar hali ham ishlamasa, VPS da:

1. Loglarni ko'rish:
   pm2 logs universalbozor-api --lines 50

2. API ni to'g'ridan-to'g'ri tekshirish:
   curl http://localhost:5000/api/stats

3. MongoDB ni tekshirish:
   node /var/www/universalbozor/server/check-today-sales.js

